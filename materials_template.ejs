```{=html}
<% for (const item of items) { %>
    <h3>Class <%= item.number %></h3>
    <p>
        <% 
            var links = [];
            if(item.materials) { links.push('<a href="' + item.materials + '">📄 Materials</a>'); }
            if(item.notes) { links.push('<a href="' + item.notes + '">📝 Notes</a>'); }
            if(item.youtube) { links.push('<a href="' + item.youtube + '">▶️ Youtube</a>'); }
            if(item.download) { links.push('<a href="' + item.download + '">💿 Download</a>'); }
            print(links.join(' • '));
        %>
    </p>
    <p><%= item.title %></p>
<% } %>
```